# ─── requirements.api.txt ────────────────────────────────────────────────────
# Used ONLY by Dockerfile.api (the FastAPI backend container).
# Keeps the API image focused.
#
# WHY SPLIT REQUIREMENTS?
#   Separating backend and frontend requirements keeps images focused.
#   Split files = faster builds + smaller images + clearer dependencies.
#
#   API image with this file:   ~2.8 GB  (ML libs are heavy)
# ─────────────────────────────────────────────────────────────────────────────

# LangChain
langchain==0.2.16
langchain-community==0.2.17
langchain-groq==0.1.9

# Embeddings + vector store
sentence-transformers==3.0.1
faiss-cpu==1.8.0.post1

# Hybrid retrieval
rank-bm25==0.2.2

# PDF + Document parsing
pymupdf==1.24.10
python-docx==1.1.2
python-pptx==1.0.2
docx2txt==0.8

# OCR (scanned PDF fallback) — requires tesseract-ocr binary on host
pytesseract==0.3.13
Pillow==10.4.0

# LLM provider
groq==0.11.0
httpx>=0.27.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Web framework
fastapi==0.114.2
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Utilities
python-dotenv==1.0.1
pydantic==2.9.2
